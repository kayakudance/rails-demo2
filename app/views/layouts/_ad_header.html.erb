<%
  if admin_signed_in?
    @auth_state = "ログインしています"
  else
    @auth_state = "ログインしていません"
  end
%>

<div style="background-color: #f2f2f2; padding-top: 30px">
  <%= link_to 'サイトへ', root_path %>
  <br>
  <%= link_to '管理者ログイン', new_admin_session_path %>
  <%= link_to '管理者ログアウト', destroy_admin_session_path, method: :delete %>
  <p>管理者ログイン状態: <%= @auth_state %></p>
  <%= link_to '管理画面TOPへ', admins_root_path %>
</div>
